{
  "name": "@aimen/skill-store",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "dev": "echo 'skill-store: dev'",
    "build": "rm -rf dist && bun build ./src/index.ts --outdir ./dist --target node --format esm --external '@aimen/*'",
    "lint": "echo 'skill-store: lint'"
  }
}