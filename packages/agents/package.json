{
  "name": "@aimen/agents",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "dev": "echo 'agents: dev'",
    "build": "rm -rf dist && bun build ./src/index.ts --outdir ./dist --target node --format esm --external '@aimen/*' --external '@mastra/*' --external zod",
    "lint": "echo 'agents: lint'"
  },
  "dependencies": {
    "@mastra/core": "^1.0.0",
    "zod": "^3.23.0",
    "@aimen/telemetry": "workspace:*"
  },
  "devDependencies": {
    "@aimen/acp-bus": "workspace:*"
  }
}