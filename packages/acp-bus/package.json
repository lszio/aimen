{
  "name": "@aimen/acp-bus",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "dev": "echo 'acp-bus: dev'",
    "build": "rm -rf dist && bun build ./src/index.ts --outdir ./dist --target node --format esm --external '@aimen/*' --external '@mastra/*' --external zod",
    "lint": "echo 'acp-bus: lint'"
  }
}